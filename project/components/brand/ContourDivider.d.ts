import * as React from 'react';
export interface ContourDividerProps { variant?: 'topo' | 'lines'; onDark?: boolean; height?: number; accent?: boolean; style?: React.CSSProperties; }
/** The signature contour divider — decorative survey/grading line work. */
export function ContourDivider(props: ContourDividerProps): JSX.Element;
