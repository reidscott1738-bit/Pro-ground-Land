import * as React from 'react';
export interface EyebrowProps { children: React.ReactNode; onDark?: boolean; as?: keyof JSX.IntrinsicElements; style?: React.CSSProperties; }
/** Uppercase Archivo section label, mirrors the logo lockup. */
export function Eyebrow(props: EyebrowProps): JSX.Element;
